userSettings:
  github_repo:   # 默认仓库
  name:  # 默认用户名
  theme: 
    default  # 默认主题
  language: 
    zh-CN  # 默认语言
    
  show_hidden_files: false  # 是否显示隐藏文件
    default_view: list  # 默认文件视图（list/grid）
     allowed_extensions:    # 允许的文件扩展名
      - .txt
      - .md
      - .json
      - .yaml
      - .yml